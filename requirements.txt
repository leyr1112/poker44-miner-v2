# Miner runtime (mirrors pyproject.toml). Install the Poker44 subnet separately:
#   pip install -e ../Poker44-subnet
bittensor
python-dotenv>=1.0
joblib>=1.3
numpy>=1.24
scikit-learn==1.9.0  # pinned: joblib estimators are version-stamped; train/serve must match
lightgbm>=4.0
